【背景音乐说明】

播放列表在 js/audio.js 的 PLAYLIST 中维护；把 mp3 放进本文件夹后，
在 PLAYLIST 里加一项即可，文件名不必固定为 bgm.mp3。

网页右下角会出现一个 ♪ 唱片按钮：
- 点击即可播放 / 暂停，播放时唱片会旋转；
- 由于浏览器安全限制，网页不允许打开就自动播放，
  但访客在页面上进行第一次点击后，音乐会自动尝试开始播放；
- 首页「此刻旋律」卡片会同步当前歌曲、进度与上一首/下一首。

温馨提示：请使用有授权或可自由分享的音源，单曲建议压缩在 10MB 以内，加载更快。
